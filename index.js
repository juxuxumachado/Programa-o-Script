//chamar a biblioteca express
const express = require('express');

//armazenar na variavel server a biblioteca express()
const server = express();

//chamar a função listen para "escutar a porta em que vamos utiliza"
server.listen(3500);
